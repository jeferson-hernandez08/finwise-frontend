/**
 * Configuración de producción.
 * Reemplaza `apiUrl` por el dominio donde publiques el backend de NestJS.
 */
export const environment = {
  production: true,
  apiUrl: 'https://TU-BACKEND.onrender.com',
};
